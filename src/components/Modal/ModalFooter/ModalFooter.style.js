import styled from 'styled-components';

const StyledModalFooter = styled.footer`
  padding: 12px 16px;
  text-align: right;
`;

const StyledModalFooterInner = styled.span`
  margin-right: 20px;
  cursor: pointer;
`;

export { StyledModalFooter, StyledModalFooterInner };
